# ===================================
# Sealed Secrets Placeholder
# ===================================
# This file should contain SealedSecret resources
# DO NOT commit plain secrets to git!
#
# Generate sealed secrets using:
#
# kubectl create secret generic realtime-poll-secrets \
#   --namespace=realtime-poll \
#   --from-literal=REDIS_PASSWORD='your-strong-password' \
#   --from-literal=SESSION_SECRET='your-32-char-session-secret' \
#   --dry-run=client -o yaml | \
#   kubeseal \
#     --controller-name=sealed-secrets-controller \
#     --controller-namespace=kube-system \
#     --format yaml > k8s/overlays/prod/sealed-secrets/secrets.yaml
#
# Example SealedSecret structure (replace with actual sealed data):
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: realtime-poll-secrets
  namespace: realtime-poll
  annotations:
    sealedsecrets.bitnami.com/cluster-wide: "false"
spec:
  encryptedData:
    # These are placeholders - replace with actual sealed values
    REDIS_PASSWORD: AgBy3i4OJSWK+PiTySYZZA9rO43cGDEq...
    SESSION_SECRET: AgBy3i4OJSWK+PiTySYZZA9rO43cGDEq...
  template:
    metadata:
      name: realtime-poll-secrets
      namespace: realtime-poll
    type: Opaque
