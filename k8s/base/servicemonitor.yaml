# ===================================
# ServiceMonitor - Prometheus Integration
# ===================================
# Allows Prometheus to scrape metrics from the backend service
# Requires Prometheus Operator (included in NKP/Kommander)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: realtime-poll-backend
  labels:
    app.kubernetes.io/name: realtime-poll
    app.kubernetes.io/component: backend
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: backend
  # Use any: true to discover services in the same namespace as the ServiceMonitor
  namespaceSelector:
    any: true
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
      scheme: http
