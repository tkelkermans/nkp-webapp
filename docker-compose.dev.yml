# ===================================
# Docker Compose - Développement avec hot-reload
# ===================================
# Usage: docker-compose -f docker-compose.dev.yml up

services:
  # Redis - Base de données
  redis:
    image: redis:7.4-alpine
    container_name: poll-redis-dev
    ports:
      - "6379:6379"
    volumes:
      - redis-data-dev:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - poll-network-dev

  # Backend - Développement avec hot-reload
  backend:
    image: node:22-alpine
    container_name: poll-backend-dev
    working_dir: /app
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/app
      - backend-node-modules:/app/node_modules
    environment:
      - NODE_ENV=development
      - PORT=3001
      - REDIS_URL=redis://redis:6379
      - CORS_ORIGIN=http://localhost:3000
      - SESSION_SECRET=dev-secret-change-in-production
    command: sh -c "npm install && npm run dev"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - poll-network-dev
    restart: unless-stopped

  # Frontend - Développement avec hot-reload
  frontend:
    image: node:22-alpine
    container_name: poll-frontend-dev
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    environment:
      - NODE_ENV=development
      # Backend URL for Next.js rewrites proxy (internal Docker network)
      - BACKEND_URL=http://backend:3001
    command: sh -c "npm install && npm run dev"
    depends_on:
      - backend
    networks:
      - poll-network-dev
    restart: unless-stopped

# Volumes
volumes:
  redis-data-dev:
    driver: local
  backend-node-modules:
    driver: local
  frontend-node-modules:
    driver: local

# Réseau
networks:
  poll-network-dev:
    driver: bridge
